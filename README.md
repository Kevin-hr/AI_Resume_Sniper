# 🎯 AI Resume Sniper (AI 简历狙击手)

> **版本**: v1.2 | **状态**: Production Ready | **作者**: yunfanai

## 1. 项目概述 (Overview)

**AI Resume Sniper** 是一个基于大语言模型（LLM）的自动化简历优化系统。它模拟一位拥有 10 年经验的资深 HRBP（人力资源业务合作伙伴）的视角，在 60 秒内对候选人的简历进行深度审计。

v1.2 版本新增了**批量处理功能**，支持批量简历筛选（招聘端）和批量职位匹配（求职端）。

### 核心价值
*   **模拟 6 秒筛选**：模拟真实 HR 的快速筛选过程，预测简历是否会被秒拒。
*   **ATS 通过率优化**：识别并填充职位描述（JD）中的关键关键词，确保通过 ATS 系统。
*   **STAR 法则重写**：自动将平淡的工作描述重写为高价值的 STAR（情境-任务-行动-结果）格式。
*   **批量高效处理**：一键处理数百份简历或职位，自动生成 CSV 格式的对比报告。
*   **多格式支持**：原生支持 PDF, DOCX, TXT, MD 等多种文档格式。

### 技术栈 (Tech Stack)
*   **核心逻辑**: Python 3.9+
*   **Web 界面**: Streamlit
*   **AI 引擎**: DeepSeek-V3 (OpenAI Compatible)
*   **文档解析**: pdfplumber, python-docx

---

## 2. 安装指南 (Installation)

### 系统要求
*   Windows / macOS / Linux
*   Python 3.9 或更高版本
*   DeepSeek API Key (或其他兼容 OpenAI SDK 的 LLM Key)

### 依赖安装
建议使用虚拟环境进行安装：

```bash
# 1. 克隆项目 (假设已下载)
cd AI_Resume_Sniper

# 2. 创建虚拟环境 (可选)
python -m venv venv
# Windows 激活:
.\venv\Scripts\activate
# Mac/Linux 激活:
source venv/bin/activate

# 3. 安装依赖 (新增 pdfplumber, python-docx)
pip install openai streamlit python-dotenv pdfplumber python-docx
```

### 环境配置
1.  在项目根目录 (`AI_Resume_Sniper`) 创建 `.env` 文件。
2.  添加 API Key：
    ```text
    DEEPSEEK_API_KEY="sk-your-key-here"
    ```

---

## 3. 使用说明 (Usage)

### 快速启动 (Web UI)
启动可视化界面，进行交互式简历诊断：

```bash
streamlit run src/web_ui.py
```
*   浏览器自动打开 `http://localhost:8501`

### 功能模式详解

#### 1️⃣ 单人模式 (Single Mode)
*   **场景**: 深度精修一份简历。
*   **操作**: 上传/粘贴简历 + 上传/粘贴 JD -> 点击分析。
*   **输出**: 深度报告、红旗预警、STAR 重写建议。

#### 2️⃣ 批量简历匹配 (Batch Resumes)
*   **场景**: HR 筛选海量候选人。
*   **操作**: 
    1.  粘贴目标 JD (Job Description)。
    2.  批量上传 N 份简历 (支持 PDF/DOCX)。
    3.  点击 "Analyze Batch Resumes"。
*   **输出**: 包含姓名、匹配分数的 CSV 表格，支持下载。

#### 3️⃣ 批量职位匹配 (Batch JDs)
*   **场景**: 求职者海投，寻找最匹配的职位。
*   **操作**:
    1.  粘贴个人简历。
    2.  批量上传 N 个 JD 文件。
    3.  点击 "Analyze Batch JDs"。
*   **输出**: 职位适配度排名，帮助优先投递高胜算职位。

### 命令行测试 (CLI)
运行集成测试脚本，生成演示报告：

```bash
python tests/integration_test.py
```

---

## 4. 开发指南 (Development)

### 项目结构
```text
AI_Resume_Sniper/
├── docs/
│   └── plans/                           # 实施计划
├── src/
│   ├── resume_sniper.py                 # [核心] AI 分析引擎类
│   ├── document_parser.py               # [新增] 文档解析器 (PDF/DOCX)
│   └── web_ui.py                        # [界面] Streamlit 前端逻辑 (含 Tabs)
├── data/
│   ├── sample_resume_A.md               # 测试用例
│   └── ...
└── tests/
    ├── integration_test.py              # 集成测试脚本
    ├── test_document_parser.py          # 解析器单元测试
    └── test_batch_logic.py              # 批量逻辑单元测试
```

### 代码规范
*   **风格**: 遵循 PEP 8。
*   **原则**: Abstract First, TDD, Side-Effect Minimization.

---

## 5. 其他信息 (Other)

### 许可证 (License)
MIT License.

### 联系方式
*   **作者**: 雲帆AI

---
> *Generated by AI Resume Sniper Project Team*
